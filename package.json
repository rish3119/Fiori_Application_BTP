{
  "name": "LearningPurpose",
  "version": "1.0.0",
  "description": "A Simple CAP Project LearningPurpose",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "start": "cds-serve"
  },
  "dependencies": {
    "@cap-js/hana": "^2",
    "@sap/cds": "^9",
    "@sap/hdi-deploy": "^5.7.0",
    "hdb": "^0.19.0"
  },
  "cds": {
    "build": {
      "tasks": [
        {
          "for": "hana",
          "dest": "../db"
        },
        {
          "for": "nodejs"
        }
      ]
    },
    "requires": {
      "db": {
        "kind": "hana-cloud"
      }
    }
  },
  "devDependencies": {
    "@sap/cds-dk": "^10.0.3"
  },
  "engines": {
  "node": "20.x",
  "npm": "10.x"
}
}
